action @show_claim_policy:
    // Todo: 
    // * find claim by @claim_number
    // * select policy_form based on claim_type
    // * show @policy_form form
end

action @show_claim:
	// Todo:
	// * find claim by @claim_list_claims
	// * populate client KVS for claim & claim_adjuster
	// * show @claim form
end